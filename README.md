# POWSYS365

POWSYS365 is a macOS-compatible power system analysis platform skeleton created for local packaging and installer generation.

## Build and install

Requirements:
- CMake 3.25+
- Apple clang / Xcode command line tools

Build and package:

```bash
./build_pkg.sh
```

This script builds a universal binary and creates a `.pkg` installer for macOS.
